apiVersion: cilium.io/v1alpha1
kind: TracingPolicy
metadata:
  name: policy-for-busybox
spec:
  podSelector:
    matchLabels:
      app: busybox
  tracepoints:
    - subsystem: "raw_syscalls"
      event: "sys_enter"
      args:
        - index: 4
          type: "int64"
